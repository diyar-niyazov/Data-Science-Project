{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "jdk",
            "request": "launch",
            "name": "Launch Java App",
            "mainClass": "Main",
            "preLaunchTask": "compile-java",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
